//
//  Secrets.swift.example
//  QuickCalories
//
//  ⚠️ TEMPLATE FILE - Copy to Secrets.swift ⚠️
//
//  Instructions:
//  1. Copy this file: cp Secrets.swift.example Secrets.swift
//  2. Replace the placeholder values below with your actual secrets
//  3. Never commit Secrets.swift to git (it's in .gitignore)

import Foundation

struct Secrets {
    // Your Vercel proxy URL (after deploying to Vercel)
    // Example: "https://your-app-name.vercel.app/api/proxy"
    static let proxyURL = "YOUR_VERCEL_PROXY_URL_HERE"
    
    // Your app secret (must match APP_SECRET in Vercel environment variables)
    // Generate a random secret:
    //   macOS: openssl rand -base64 32
    //   Or use any random string generator
    static let appSecret = "YOUR_APP_SECRET_HERE"
}
